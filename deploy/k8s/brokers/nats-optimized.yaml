# NATS JetStream – single-node optimized for low latency
apiVersion: v1
kind: Service
metadata:
  name: nats-optimized
  labels:
    app: nats-optimized
    benchmark.ninefinger9.io/broker-id: nats
spec:
  selector:
    app: nats-optimized
  ports:
    - name: client
      port: 4222
      targetPort: 4222
    - name: monitor
      port: 8222
      targetPort: 8222
  clusterIP: None
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nats-optimized
  labels:
    app: nats-optimized
    benchmark.ninefinger9.io/broker-id: nats
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nats-optimized
  template:
    metadata:
      labels:
        app: nats-optimized
        benchmark.ninefinger9.io/broker-id: nats
    spec:
      containers:
        - name: nats
          image: nats:2.11.3-alpine
          args:
            - "--jetstream"
            - "--store_dir=/data"
            - "--max_payload=1MB"
            - "--write_deadline=2s"
          ports:
            - containerPort: 4222
            - containerPort: 8222
          resources:
            requests:
              cpu: "2"
              memory: 4Gi
            limits:
              cpu: "2"
              memory: 4Gi
          volumeMounts:
            - name: data
              mountPath: /data
      volumes:
        - name: data
          emptyDir: {}
